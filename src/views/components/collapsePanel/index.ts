import collapsePanel from './collapsePanel.vue'

/* istanbul ignore next */
collapsePanel.install = function (Vue:any) {
  Vue.component(collapsePanel.name, collapsePanel)
}

export default collapsePanel
